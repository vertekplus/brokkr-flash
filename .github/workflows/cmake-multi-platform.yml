name: Build & Release

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:
    inputs:
      new_tag:
        description: "New release tag (e.g. v1.4.0). Leave empty for build-only."
        required: false
        type: string
        default: ""
      prev_tag:
        description: "Previous release tag for changelog (e.g. v1.3.0)."
        required: false
        type: string
        default: ""

permissions:
  contents: write

env:
  QT_VERSION: "6.7.3"

jobs:
  # ═══════════════════════════════════════════════════
  #  Bump version, commit, tag  (release only)
  # ═══════════════════════════════════════════════════
  prepare:
    name: Bump version & tag
    if: inputs.new_tag != ''
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract semver from tag
        id: ver
        run: |
          TAG="${{ inputs.new_tag }}"
          VER="${TAG#v}"
          echo "version=$VER" >> "$GITHUB_OUTPUT"

      - name: Update CMakeLists.txt version
        run: |
          sed -i "s/project(Brokkr LANGUAGES C CXX VERSION [0-9.]*)/project(Brokkr LANGUAGES C CXX VERSION ${{ steps.ver.outputs.version }})/" CMakeLists.txt
          grep 'project(Brokkr' CMakeLists.txt

      - name: Commit & tag
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add CMakeLists.txt
          git commit -m "Release ${{ inputs.new_tag }}"
          git tag "${{ inputs.new_tag }}"
          git push origin main --tags

  # ═══════════════════════════════════════════════════
  #  Linux – Ubuntu 24.04, AppImage (x64)
  # ═══════════════════════════════════════════════════
  linux_appimage:
    name: Linux AppImage x64 (${{ matrix.build_type }})
    needs: [prepare]
    if: always() && (needs.prepare.result == 'success' || needs.prepare.result == 'skipped')
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        include:
          - build_type: Release
            suffix: ""
          - build_type: Debug
            suffix: "-dbg"

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.new_tag || github.ref }}

      - name: Install build deps
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y \
            gcc-14 g++-14 ninja-build cmake patchelf \
            libgl1-mesa-dev libfuse2 imagemagick wget file

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          cache: true
          cache-key-prefix: install-qt-action-${{ github.job }}-${{ matrix.build_type }}
          arch: linux_gcc_64

      - name: Configure
        run: |
          set -euo pipefail
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
            -DCMAKE_C_COMPILER=gcc-14 \
            -DCMAKE_CXX_COMPILER=g++-14 \
            -DCMAKE_PREFIX_PATH="$QT_ROOT_DIR"

      - name: Build
        run: cmake --build build --parallel 2

      - name: Stage AppDir
        run: |
          set -euo pipefail
          rm -rf AppDir
          cmake --install build --prefix "$PWD/AppDir/usr"

          mkdir -p AppDir/usr/lib
          cp "$(g++-14 -print-file-name=libstdc++.so.6)" AppDir/usr/lib/ || true
          cp "$(gcc-14 -print-file-name=libgcc_s.so.1)"  AppDir/usr/lib/ || true

      - name: Desktop file + icon
        run: |
          set -euo pipefail
          mkdir -p AppDir/usr/share/applications \
                   AppDir/usr/share/icons/hicolor/256x256/apps

          cat > AppDir/usr/share/applications/brokkr.desktop << 'DESKTOP'
          [Desktop Entry]
          Type=Application
          Name=Brokkr
          Exec=brokkr
          Icon=brokkr
          Categories=Utility;
          Terminal=false
          DESKTOP
          sed -i 's/^[[:space:]]*//' AppDir/usr/share/applications/brokkr.desktop

          convert assets/brokkr.ico /tmp/brokkr_frame.png
          biggest="$(ls -rS /tmp/brokkr_frame*.png | tail -n1)"
          convert "$biggest" -resize 256x256! \
            AppDir/usr/share/icons/hicolor/256x256/apps/brokkr.png
          identify AppDir/usr/share/icons/hicolor/256x256/apps/brokkr.png

      - name: Build AppImage
        run: |
          set -euo pipefail
          wget -q "https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage"
          wget -q "https://github.com/linuxdeploy/linuxdeploy-plugin-qt/releases/download/continuous/linuxdeploy-plugin-qt-x86_64.AppImage"
          chmod +x linuxdeploy-x86_64.AppImage linuxdeploy-plugin-qt-x86_64.AppImage

          export PATH="$QT_ROOT_DIR/bin:$PATH"
          export QMAKE="$QT_ROOT_DIR/bin/qmake"

          ./linuxdeploy-x86_64.AppImage \
            --appdir AppDir \
            -e AppDir/usr/bin/brokkr \
            -d AppDir/usr/share/applications/brokkr.desktop \
            -i AppDir/usr/share/icons/hicolor/256x256/apps/brokkr.png \
            --plugin qt \
            --output appimage

          mv Brokkr-x86_64.AppImage Brokkr-linux-x86_64${{ matrix.suffix }}.AppImage

      - uses: actions/upload-artifact@v4
        with:
          name: Brokkr-linux-x86_64${{ matrix.suffix }}.AppImage
          path: Brokkr-linux-x86_64${{ matrix.suffix }}.AppImage

  # ═══════════════════════════════════════════════════
  #  Linux – Ubuntu 24.04, AppImage (arm64)
  # ═══════════════════════════════════════════════════
  linux_appimage_arm64:
    name: Linux AppImage arm64 (${{ matrix.build_type }})
    needs: [prepare]
    if: always() && (needs.prepare.result == 'success' || needs.prepare.result == 'skipped')
    runs-on: ubuntu-24.04-arm
    strategy:
      matrix:
        include:
          - build_type: Release
            suffix: ""
          - build_type: Debug
            suffix: "-dbg"

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.new_tag || github.ref }}

      - name: Install build deps
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y \
            gcc-14 g++-14 ninja-build cmake patchelf \
            libgl1-mesa-dev libfuse2 imagemagick wget file

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          cache: true
          cache-key-prefix: install-qt-action-${{ github.job }}-${{ matrix.build_type }}
          arch: linux_gcc_arm64

      - name: Configure
        run: |
          set -euo pipefail
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
            -DCMAKE_C_COMPILER=gcc-14 \
            -DCMAKE_CXX_COMPILER=g++-14 \
            -DCMAKE_PREFIX_PATH="$QT_ROOT_DIR"

      - name: Build
        run: cmake --build build --parallel 2

      - name: Stage AppDir
        run: |
          set -euo pipefail
          rm -rf AppDir
          cmake --install build --prefix "$PWD/AppDir/usr"

          mkdir -p AppDir/usr/lib
          cp "$(g++-14 -print-file-name=libstdc++.so.6)" AppDir/usr/lib/ || true
          cp "$(gcc-14 -print-file-name=libgcc_s.so.1)"  AppDir/usr/lib/ || true

      - name: Desktop file + icon
        run: |
          set -euo pipefail
          mkdir -p AppDir/usr/share/applications \
                   AppDir/usr/share/icons/hicolor/256x256/apps

          cat > AppDir/usr/share/applications/brokkr.desktop << 'DESKTOP'
          [Desktop Entry]
          Type=Application
          Name=Brokkr
          Exec=brokkr
          Icon=brokkr
          Categories=Utility;
          Terminal=false
          DESKTOP
          sed -i 's/^[[:space:]]*//' AppDir/usr/share/applications/brokkr.desktop

          convert assets/brokkr.ico /tmp/brokkr_frame.png
          biggest="$(ls -rS /tmp/brokkr_frame*.png | tail -n1)"
          convert "$biggest" -resize 256x256! \
            AppDir/usr/share/icons/hicolor/256x256/apps/brokkr.png
          identify AppDir/usr/share/icons/hicolor/256x256/apps/brokkr.png

      - name: Build AppImage
        run: |
          set -euo pipefail
          wget -q "https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-aarch64.AppImage"
          wget -q "https://github.com/linuxdeploy/linuxdeploy-plugin-qt/releases/download/continuous/linuxdeploy-plugin-qt-aarch64.AppImage"
          chmod +x linuxdeploy-aarch64.AppImage linuxdeploy-plugin-qt-aarch64.AppImage

          export PATH="$QT_ROOT_DIR/bin:$PATH"
          export QMAKE="$QT_ROOT_DIR/bin/qmake"

          ./linuxdeploy-aarch64.AppImage \
            --appdir AppDir \
            -e AppDir/usr/bin/brokkr \
            -d AppDir/usr/share/applications/brokkr.desktop \
            -i AppDir/usr/share/icons/hicolor/256x256/apps/brokkr.png \
            --plugin qt \
            --output appimage

          mv Brokkr-aarch64.AppImage Brokkr-linux-arm64${{ matrix.suffix }}.AppImage

      - uses: actions/upload-artifact@v4
        with:
          name: Brokkr-linux-arm64${{ matrix.suffix }}.AppImage
          path: Brokkr-linux-arm64${{ matrix.suffix }}.AppImage

  # ═══════════════════════════════════════════════════
  #  Windows x64 + arm64 (NSIS installer + portable zip)
  # ═══════════════════════════════════════════════════
  windows_installer:
    name: Windows installer ${{ matrix.artifact_arch }} (${{ matrix.build_type }})
    needs: [prepare]
    if: always() && (needs.prepare.result == 'success' || needs.prepare.result == 'skipped')
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        include:
          - runner: windows-2022
            artifact_arch: x64
            msvc_arch: x64
            qt_arch: win64_msvc2019_64
            build_type: Release
            suffix: ""
          - runner: windows-2022
            artifact_arch: x64
            msvc_arch: x64
            qt_arch: win64_msvc2019_64
            build_type: Debug
            suffix: "-dbg"
          - runner: windows-11-arm
            artifact_arch: arm64
            msvc_arch: arm64
            qt_arch: win64_msvc2019_arm64
            build_type: Release
            suffix: ""
          - runner: windows-11-arm
            artifact_arch: arm64
            msvc_arch: arm64
            qt_arch: win64_msvc2019_arm64
            build_type: Debug
            suffix: "-dbg"

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.new_tag || github.ref }}

      - uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: ${{ matrix.msvc_arch }}

      - name: Install NSIS + Ninja
        shell: powershell
        run: choco install nsis ninja -y

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          cache: true
          cache-key-prefix: install-qt-action-${{ github.job }}-${{ matrix.artifact_arch }}-${{ matrix.build_type }}
          host: windows
          arch: ${{ matrix.qt_arch }}

      - name: Configure
        shell: bash
        run: |
          set -euo pipefail
          echo "$QT_ROOT_DIR/bin" >> "$GITHUB_PATH"
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
            "-DCMAKE_PREFIX_PATH=$QT_ROOT_DIR"

      - name: Build
        shell: bash
        run: cmake --build build --parallel 2

      - name: Package (NSIS installer)
        shell: bash
        run: |
          set -euo pipefail
          cd build
          cpack -G NSIS -C ${{ matrix.build_type }} || {
            echo "::error::NSIS packaging failed – dumping log:"
            cat _CPack_Packages/*/NSIS/NSISOutput.log 2>/dev/null || true
            exit 1
          }
          installer="$(ls -1 *.exe | head -n1)"
          mv "$installer" ../Brokkr-windows-${{ matrix.artifact_arch }}${{ matrix.suffix }}.exe

      - name: Package (portable zip)
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p portable/brokkr
          cp build/brokkr.exe portable/brokkr/
          "$QT_ROOT_DIR/bin/windeployqt.exe" portable/brokkr/brokkr.exe
          cd portable
          7z a -tzip ../Brokkr-windows-${{ matrix.artifact_arch }}${{ matrix.suffix }}.zip brokkr/

      - uses: actions/upload-artifact@v4
        with:
          name: Brokkr-windows-${{ matrix.artifact_arch }}${{ matrix.suffix }}.exe
          path: Brokkr-windows-${{ matrix.artifact_arch }}${{ matrix.suffix }}.exe

      - uses: actions/upload-artifact@v4
        with:
          name: Brokkr-windows-${{ matrix.artifact_arch }}${{ matrix.suffix }}.zip
          path: Brokkr-windows-${{ matrix.artifact_arch }}${{ matrix.suffix }}.zip

  # ═══════════════════════════════════════════════════
  #  macOS arm64
  # ═══════════════════════════════════════════════════
  macos_app:
    name: macOS app arm64 (${{ matrix.build_type }})
    needs: [prepare]
    if: always() && (needs.prepare.result == 'success' || needs.prepare.result == 'skipped')
    runs-on: macos-15
    strategy:
      matrix:
        include:
          - build_type: Release
            suffix: ""
          - build_type: Debug
            suffix: "-dbg"

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.new_tag || github.ref }}

      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Install Ninja
        run: brew install ninja

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          cache: true
          cache-key-prefix: install-qt-action-${{ github.job }}-${{ matrix.build_type }}
          arch: clang_64

      - name: Configure
        run: |
          set -euo pipefail
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
            -DCMAKE_PREFIX_PATH="$QT_ROOT_DIR" \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=13.0

      - name: Strip missing AGL framework from build files
        run: |
          set -euo pipefail
          sed -i '' 's/ -framework AGL//g; s/-framework AGL //g; s/-framework AGL//g' build/build.ninja

      - name: Build
        run: cmake --build build --parallel 3

      - name: Deploy + Zip
        if: matrix.build_type == 'Release'
        run: |
          set -euo pipefail
          APP="build/brokkr.app"
          "$QT_ROOT_DIR/bin/macdeployqt" "$APP" -verbose=2
          ditto -c -k --sequesterRsrc --keepParent "$APP" Brokkr-macos-arm64${{ matrix.suffix }}.app.zip

      - name: Deploy + Zip (debug)
        if: matrix.build_type == 'Debug'
        run: |
          set -euo pipefail
          APP="build/brokkr.app"
          "$QT_ROOT_DIR/bin/macdeployqt" "$APP" -verbose=2
          xattr -cr "$APP" || true
          ditto -c -k --sequesterRsrc --keepParent "$APP" Brokkr-macos-arm64${{ matrix.suffix }}.app.zip

      - uses: actions/upload-artifact@v4
        with:
          name: Brokkr-macos-arm64${{ matrix.suffix }}.app.zip
          path: Brokkr-macos-arm64${{ matrix.suffix }}.app.zip

  # ═══════════════════════════════════════════════════
  #  macOS x86_64 (Big Sur+)
  # ═══════════════════════════════════════════════════
  macos_app_x86:
    name: macOS app x86_64 (${{ matrix.build_type }})
    needs: [prepare]
    if: always() && (needs.prepare.result == 'success' || needs.prepare.result == 'skipped')
    runs-on: macos-15-intel
    strategy:
      matrix:
        include:
          - build_type: Release
            suffix: ""
          - build_type: Debug
            suffix: "-dbg"

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.new_tag || github.ref }}

      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Install Ninja
        run: brew install ninja

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          cache: true
          cache-key-prefix: install-qt-action-${{ github.job }}-${{ matrix.build_type }}
          arch: clang_64

      - name: Configure
        run: |
          set -euo pipefail
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
            -DCMAKE_PREFIX_PATH="$QT_ROOT_DIR" \
            -DCMAKE_OSX_ARCHITECTURES=x86_64 \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=11.0

      - name: Strip missing AGL framework from build files
        run: |
          set -euo pipefail
          sed -i '' 's/ -framework AGL//g; s/-framework AGL //g; s/-framework AGL//g' build/build.ninja

      - name: Build
        run: cmake --build build --parallel 3
        
      - name: Deploy + Zip
        if: matrix.build_type == 'Release'
        run: |
          set -euo pipefail
          APP="build/brokkr.app"
          "$QT_ROOT_DIR/bin/macdeployqt" "$APP" -verbose=2
          ditto -c -k --sequesterRsrc --keepParent "$APP" Brokkr-macos-x86_64${{ matrix.suffix }}.app.zip

      - name: Deploy + Zip (debug)
        if: matrix.build_type == 'Debug'
        run: |
          set -euo pipefail
          APP="build/brokkr.app"
          "$QT_ROOT_DIR/bin/macdeployqt" "$APP" -verbose=2
          xattr -cr "$APP" || true
          ditto -c -k --sequesterRsrc --keepParent "$APP" Brokkr-macos-x86_64${{ matrix.suffix }}.app.zip


      - uses: actions/upload-artifact@v4
        with:
          name: Brokkr-macos-x86_64${{ matrix.suffix }}.app.zip
          path: Brokkr-macos-x86_64${{ matrix.suffix }}.app.zip

  # ═══════════════════════════════════════════════════
  #  Publish GitHub Release  (release only)
  # ═══════════════════════════════════════════════════
  release:
    name: Publish release
    if: inputs.new_tag != ''
    needs: [prepare, linux_appimage, linux_appimage_arm64, windows_installer, macos_app, macos_app_x86]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.new_tag }}
          fetch-depth: 0

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Generate changelog body
        run: |
          NEW="${{ inputs.new_tag }}"
          PREV="${{ inputs.prev_tag }}"

          echo "## Brokkr $NEW" > body.md
          echo "" >> body.md

          if [ -n "$PREV" ]; then
            git log "${PREV}..${NEW}" --pretty=format:"- %s" --no-merges >> body.md
            echo "" >> body.md
            echo "" >> body.md
            echo "**Full Changelog**: https://github.com/${{ github.repository }}/compare/${PREV}...${NEW}" >> body.md
          else
            echo "_First release_" >> body.md
          fi

          cat body.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.new_tag }}
          name: Brokkr ${{ inputs.new_tag }}
          body_path: body.md
          draft: false
          prerelease: false
          files: artifacts/*